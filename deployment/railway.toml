[build]
builder = "DOCKERFILE"
dockerfilePath = "deployment/Dockerfile"

[deploy]
startCommand = "uvicorn powerbot.app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
